<%= provide(:title, "会員登録") %>
<div class="row">
  <div class="form col-xs-11">
    <%= form_with(model: @user,url: {controller: 'users', action: 'create' }, local: true) do |f| %>
      <%= render 'shared/error_messages' %>

      <%= f.label :name, "ニックネーム：", class: "form-label" %>
      <%= f.text_field :name%>
      <p>10文字以内(家族内でどう呼び合ってる？)</p>
      <%# label for="user_name" %>
      <%# input id="user_name" name="user[name]" type="text" %>
      <%# name="user[name]" => Controllerにてparams[:user][:name]として取り出す %>

      <%= f.label :email, "メールアドレス：" %>
      <%= f.email_field :email%>
      <p>このメールアドレスを使って会員認証します</p>

      <%= f.label :password, "パスワード：" %>
      <%= f.password_field :password%>
      <p>8文字以上20文字以内</p>

      <%= f.label :password_confirmation, "パスワード（確認）：" %>
      <%= f.password_field :password_confirmation%>
      <p></p>

      <%= f.submit "会員登録(メール認証)" ,class: "btn standerd-btn btn-primary" %>

    <% end %>
    <%= link_to "登録済みの方はこちら", "#", class: "moderate" %>
  </div>
</div>